#!/bin/bash
# in order to stop python when vscode debugger stopped, please use source to run this script
# before start, any operation can do, such as source environment variables
# gdbserver :$GDBSERVER_PORT $APP_NAME # start gdbserver
exit 0 # for python invoke_shell exit signal